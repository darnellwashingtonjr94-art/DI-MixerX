# DI-MixerX
DI-MixerX is a defensive honeypot designed to generate dynamically spoofed device identifiers. By randomly altering hardware and network data like IP addresses, MAC addresses, uptimes and build numbers, it creates a trap for malicious actors. It confuses hackers by feeding them fake confidential information, ensuring they download only bogus data.
